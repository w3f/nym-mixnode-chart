apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mixnode.fullname" . }}-test-connection"
  labels:
    {{- include "mixnode.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mixnode.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
